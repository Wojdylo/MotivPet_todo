# Ikony aplikacji MotivPet

Aby wygenerować ikony dla Androida:

1. Umieść plik `icon.png` (1024x1024px) w katalogu `resources/`
2. Uruchom: `npx capacitor-assets generate`

Lub użyj domyślnych ikon SVG w folderze `public/`
